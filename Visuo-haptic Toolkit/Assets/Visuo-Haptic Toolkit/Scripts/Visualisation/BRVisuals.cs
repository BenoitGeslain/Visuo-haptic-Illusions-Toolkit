using UnityEngine;

using BG.Redirection;

namespace BG.Visualisation {
	public class BRVisuals : MonoBehaviour {

		// [SerializeField] private GameObject BRManager;

		private void Start() {
			// Debug.Log(GameObject.FindObjectOfType(typeof(BodyRedirection)));
			// BRManager = GameObject.FindObjectOfType(typeof(BodyRedirection));
		}
	}
}
